using System;

using Curan.Common.system.io;

using UnityEngine;

namespace Curan.Common.FormalizedData.File.Mmd.Pmd
{
	/*
	public class PmdMaterialChunk : PmdChunkAbstract
	{
		public PmdMaterialChunk( ByteArray aByteArray )
		{
			count = aByteArray.ReadUInt32();

			dataArray = new PmdMaterialData[count];

			for( int i = 0; i < count; i++ )
			{
				dataArray[i] = new PmdMaterialData( aByteArray );
			}
		}
	}*/
}
